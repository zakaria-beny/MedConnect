server:
  port: 8080
  address: 0.0.0.0

spring:
  application:
    name: api-gateway

  main:
    web-application-type: reactive



  cloud:
    gateway:
      metrics:
        enabled: true
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

      routes:
        # ✅ Block any internal endpoints from outside (FIXED PATTERN)
        - id: block-internal
          uri: no://op
          predicates:
            # ❌ was /api/**/internal/** (invalid in Spring Boot 3 / PathPatternParser)
            # ✅ valid pattern: one segment between /api and /internal
            - Path=/api/*/internal/**
          filters:
            - SetStatus=403

        # ✅ Auth / Users
        - id: user-service
          uri: lb://user-service
          predicates:
            - Path=/api/users/**,/api/auth/**

        - id: dmp-service
          uri: lb://dmp-service
          predicates:
            - Path=/api/dmp/**

        # ✅ Prescription Service (NEWLY ADDED)
        - id: prescription-service
          uri: lb://prescription-service
          predicates:
            - Path=/api/prescriptions/**

      # ✅ CORS (Web + Mobile)
      globalcors:
        add-to-simple-url-handler-mapping: true
        corsConfigurations:
          '[/**]':
            allowedOriginPatterns:
              - "http://localhost:*"
              - "http://127.0.0.1:*"
            allowedMethods: [GET, POST, PUT, DELETE, OPTIONS, PATCH]
            allowedHeaders: ["Authorization", "Content-Type", "X-Requested-With"]
            exposedHeaders: ["Authorization"]
            allowCredentials: true

# ✅ Your custom config
medconnect:
  security:
    jwt-secret: ${JWT_SECRET:medconnect-super-secret-key-that-is-at-least-32-chars-long}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics

  endpoint:
    health:
      show-details: always

  prometheus:
    metrics:
      export:
        enabled: true
  metrics:
    tags:
      application: ${spring.application.name}
    distribution:
      percentiles-histogram:
        http.server.requests: true
      percentiles:
        http.server.requests: 0.5, 0.90, 0.95, 0.99
